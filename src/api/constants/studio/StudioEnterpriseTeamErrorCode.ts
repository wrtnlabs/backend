export const enum StudioEnterpriseTeamErrorCode {
  /**
   * Role Deficiency.
   *
   * A task that is impossible to perform due to a low role level.
   */
  INSUFFICIENT_ROLE = "STUDIO_ENTERPRISE_TEAM_COMPANION_INSUFFICIENT_ROLE",
}
